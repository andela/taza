deployment:
  production:
    branch: master
    commands:
      - git push git@heroku.com:andela-taza.git $CIRCLE_SHA1:refs/heads/master
      - heroku run rake db:migrate --app andela-taza:
          timeout: 400 # if your deploys take a long time